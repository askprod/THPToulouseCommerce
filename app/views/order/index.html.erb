<% if @all_orders.exists? %>

Your previous orders:
<br><br>
<% @all_orders.each do |f| %>
    Order Reference: <%= f.id %>
    <br>
        <% f.items.each do |item| %>
            <%= item.title %>
            <br>
            <%= item.description %>
            <br>
            <%= item.price %>
            <br>
        <% end %>
        <br>
<% end %>

<% else %>
You haven't passed any orders yet.
<% end %>
